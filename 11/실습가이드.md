# 11. Fronthaul byte budget + delay

## 목표
RB만이 아니라 **fronthaul 전송 capacity(TTI당 byte 상한)** 병목을 본다.

## 모델
- `FRONT_BUDGET` bytes/TTI 초과 시 할당 clip
- clip 발생 TTI·penalty 누적

## 비교
`budget_ON` vs `budget_OFF`

## 실습 과제
1. `FRONT_BUDGET`를 키우며 `total_served` 회복 곡선
2. clipped TTI 비율과 큐 잔량 상관
