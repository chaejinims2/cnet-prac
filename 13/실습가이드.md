# 13. Trace player (CQI·arrival CSV)

## 목표
`rand()` 없이 **trace.csv**로 CQI·arrival을 재생해 **재현 가능 KPI**를 만든다.

## trace.csv
```
tti,ue0_cqi,ue1_cqi,ue2_cqi,ue0_arr,ue1_arr,ue2_arr
```

## 실습 과제
1. trace 행을 바꿔 동일 seed 없이도 동일 KPI 재현
2. 05 arrival load trace를 붙여 RR/PF 비교 (선택)
